# Codefront 开发状态与验收记录
记录日期：2026-09-13 / 阶段：设计完成，准备实现

## 当前状态
需求已确认：Python 为主、多语言、自选教学、真实实时演示、二人以上联机、GitHub 关联、Sites 部署、docx 文档归档。第一版简化为 2–4 人公开视野休闲房间。
已完成：读取 Sites 能力与托管约束；初始化 Sites starter；确认目标 GitHub 仓库为空；设置 origin；生成产品架构、实现手册与开发计划原稿；Sites 关联已写入 .openai/hosting.json。
进行中：依赖安装、DOCX 排版与检查。产品代码、多人 API、浏览器 Python 验证尚未完成。当前不能称为可玩版本或已部署游戏。
架构决定：浏览器 Python/JS Worker + Sites Worker 云端规则 + D1 房间；第一版不提供严格服务器代码执行认证。公开视野、三分钟、一张地图、三角色。

## 验收清单与后续目标
待验收：规则回归；正常 Python 执行；死循环隔离；代码修改影响行为；暂停与重置；三客户端联机；越权与重复动作；刷新恢复；移动布局；GitHub 推送；CI；Sites 终态与线上完整流程。
已知限制：Python 运行需首次下载运行时；客户端运行不能用于严格防作弊；HTTP 快照同步需要实测延迟；D1 不适合无限扩张高频对局；私有 Sites 的朋友需要访问权限。
后续：完成 M1–M4 并更新本文件，将测试记录写成执行命令、结果、证据位置；明确标记没有执行的验收。M5 服务器沙箱、8 人团队和完整回放继续列为计划。
